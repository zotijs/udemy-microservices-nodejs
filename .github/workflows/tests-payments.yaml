name: tests-payments

on:
  pull_request:
    paths:
      - "payments/auth/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd ticketing/payments && npm install && npm run test:ci
